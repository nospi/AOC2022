module.exports = {

    mapInput: (input) => {
        const lines = input.split("\n").filter(line => line.length)
        return lines;
    },

    solve1: (input) => {
        return;
    },

    solve2: (input) => {
        return;
    },

}